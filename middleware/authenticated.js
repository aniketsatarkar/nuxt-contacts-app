export default function (context) {
  // const token = context.app.$cookies.get('token');
  // if(token !== undefined && token !== null) {
  //   context.store.dispatch('markAsLoggedIn');
  //   context.redirect('/home');
  // }
  // else {
  //   context.store.dispatch('markAsLoggedOut');
  //   context.redirect('/');
  // }
}
